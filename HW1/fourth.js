function add(a, b) {
  return (a + b)
}

function minus(a, b) {
  return (a - b)
}

function multiply(a, b) {
  return (a * b)
}

function section(a, b) {
  return (a / b)
}

sum = add(a, b);
dif = minus(a, b);
intersec = multiply(a, b);
fraction = section(a, b);
alert(`Сумма=${sum} ,Разность= ${dif} Произведение= ${intersec} Частное= ${fraction}`)
